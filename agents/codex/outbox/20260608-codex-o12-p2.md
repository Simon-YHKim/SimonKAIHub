# Codex O-12 Phase C P2 Polish

- Worktree: `E:/Coding Infra/_worktrees/2ndB-o12-p2-polish`
- Branch: `codex/o12-p2-polish`
- Base: `3ff80f6 docs(orders): Phase C P0/P1 all fixed and live; P2 polish remains`

## Commits

1. `0becdcd refactor(onboarding): remove dead code + GB progress dots`
   - Removed unreachable `village` and `secondb` onboarding art branches plus unused local styles.
   - Swapped progress dots to `gameboy.radius`, `gameboy.border`, and `gameboy.power`.
   - Hid the top Skip action on the last step while preserving the secondary last-step skip action.

2. `fa2036c feat(ui): pixel loading indicator`
   - Replaced `PremiumLoadingState` `ActivityIndicator` with a Game Boy pixel dot-matrix blink glyph.
   - Preserved `title={message ?? t("states.loading")}` for `check:constraints`.
   - Reduced motion renders the glyph statically.

3. `92467ea fix(type): capture body input readable font`
   - Empty commit by design. `src/app/capture.tsx` already had `fontFamily: fontFamilies.readable` at branch base (`3ff80f6`, line 1378), so no file churn was added.

## Verification

- `npm run verify`: PASS
- Install note: fresh worktree had no `node_modules`; `npm ci` was blocked by the pre-existing package-lock/package mismatch, so verification dependencies were installed with `npm install --no-package-lock --legacy-peer-deps --no-audit --no-fund`.
- Lint note: verify emitted one pre-existing warning in `src/components/graph/NavGraph.tsx:723` for an unused eslint-disable directive.

## Scope Check

- Changed files across non-empty commits: `src/app/onboarding.tsx`, `src/components/premium/feedback.tsx`
- `src/components/graph/NavGraph.tsx`: untouched
- Working tree after verify: clean
