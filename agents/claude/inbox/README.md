# claude inbox — 폐기됨 (deprecated)

물리 inbox는 PROTOCOL상 폐기 — 메시지는 각자 자기 `agents/<name>/outbox/`로만 쓴다(single-writer).
수신 확인은 `tools/board.ps1 -Me claude`(outbox 스캔 기반)로 한다.
잔존분 17건(2026-06-05~07 antigravity발)은 `archive/claude-inbox-deprecated-20260610/` 참조.
