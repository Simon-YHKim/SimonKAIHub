# 2026-06-08 Codex O-10 IA Density

## 변경 요약
- 새 worktree: `E:/Coding Infra/_worktrees/2ndB-o10-ia-density`
- 브랜치: `codex/o10-ia-density`
- 원자 커밋 4개:
  - `0bddde4 refactor(capture): prioritize entry input`
  - `9f7b6d3 refactor(secondb): prioritize message input`
  - `2f0862d refactor(profile): center subscription status`
  - `9e7b237 test(nav): keep settings under profile`

## 화면별 Before/After

### 1. 당기 `/capture`
- Before: 첫 화면에 SceneHero, 형식 관리, 위키 트랙, 모드 탭, 일기 프롬프트, 태그 카드가 함께 노출됨.
- After: 첫 화면은 작은 기록 헤더, 본문 입력, 저장 버튼, More disclosure만 노출. 모드, 트랙, 태그, 프롬프트, 주제, 결론, SecondB 반영 옵션은 More 탭 후 노출.
- 접근성: 숨긴 hero 설명 키는 화면 텍스트로 재노출하지 않고 primary header의 accessibility label/hint로 유지.

### 2. 세컨비 `/secondb`
- Before: 큰 SceneHero와 사용량 패널이 입력창보다 먼저 보이고, 메시지 입력창은 화면 하단에 위치.
- After: 상단은 `SecondB + 사용량 + 모드` 1줄로 축소. 메시지 입력창을 그 바로 아래 1차 동선으로 이동. 대화가 시작되면 SceneHero는 렌더하지 않고 작은 SecondB sprite만 헤더에 남김.
- 사용량 한도 도달 시 플랜 진입은 입력창 아래 보조 링크로 유지.

### 3. 나/프로필 `/profile`
- Before: 큰 SceneHero, 계정 카드, 4개 섹션 총 21개 항목이 한 번에 노출됨.
- After: 1차 카드는 구독 상태 카드. 헤더 우측의 단일 gear icon으로 설정 진입. 하단 quick actions는 `coreBrain`, `esm`, `persona`, `insights` 4개만 노출.
- 계정 이메일은 보조 strip으로 낮춤.

### 4. 네비 허브
- expo-router 구조 확인 결과 `app/(tabs)`가 아니라 flat `Stack` + custom `PremiumTabBar`.
- 기존 `/settings` Stack route는 유지.
- 하단 탭 경로는 `["/", "/capture", "/secondb", "/profile"]` 그대로 유지.
- `src/lib/nav/tabs.ts`에 `PROFILE_CHILD_PATHS = ["/settings"]`를 추가해 설정이 프로필 하위 1-depth route임을 코드로 고정.
- Jest 테스트 `src/lib/nav/__tests__/tabs.test.ts` 추가. `/settings`가 primary tab이 아니고 profile child route임을 검증.
- 홈/그래프/NavGraph는 수정하지 않음.

## Verify
- `npm ci`: lock 불일치로 실패. package-lock을 수정하지 않기 위해 `E:/2ndB/node_modules`를 새 worktree의 `node_modules` junction으로 연결.
- `npm run verify`: PASS
  - lint PASS
  - type-check PASS
  - check:i18n PASS
  - check:lexicon PASS
  - check:llm-boundary PASS
  - check:constraints PASS
  - check:emdash PASS
  - jest PASS, 103 suites, 888 tests
