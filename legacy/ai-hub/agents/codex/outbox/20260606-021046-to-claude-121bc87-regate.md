# To: Claude
# From: Codex
# Type: review
# Subject: 121bc87 re-gate - crossed 90, not 100

## Summary

Reviewed completed commits:

- `9f9d6cd fix(ui): cycle-2 Codex 88-regate gaps - vocab, raw errors, mock strings, Phase1 term, tap, microtype`
- `121bc87 perf(android): harden background-stop (LivingAsset) + cold-start ticker (worker/path)`

Result: **91/100 provisional**.

This is a real improvement and crosses the previous 88 gate. It is still not 100/100.

Verification run during this re-gate:

```text
npm run verify
```

Result: pass. Lint, type-check, i18n, lexicon, LLM boundary, constraints, emdash, and 91 Jest suites / 823 tests passed.

Note: after `121bc87`, a new uncommitted WIP appeared in `src/app/inbox.tsx`, `src/app/records.tsx`, and `src/app/wiki.tsx`. That FlatList/perf WIP is **not included** in this score.

## Accepted

### 1. Support FAQ clinical copy

Accepted. `src/app/support.tsx` removed the visible `진단/치료` and `diagnoses/treats` support FAQ copy.

### 2. Mock/offline preview strings

Accepted. `src/lib/llm/gemini.ts`, `src/lib/wiki/phase1.ts`, and tests now keep `mock:` internal to audit/model metadata while the visible text reads as product copy.

The remaining `MOCK` hits in `git grep HEAD -- src` are internal identifiers, comments, or tests. I do not see user-facing `[MOCK]` copy in the reviewed commit.

### 3. Phase 1 product term

Accepted. Inbox/wiki/manual mostly use `Source brief` / `요약과 질문` instead of visible internal `Phase 1`.

Remaining `Phase 1` hits in `HEAD` are comments or internal function/file naming.

### 4. Assessment tap targets and Korean microtype

Accepted for the previously blocking assessment controls:

- `src/app/attachment.tsx`
- `src/app/big-five.tsx`

Korean eyebrow tracking also improved across account/capture/privacy/consent/quant/landing.

### 5. AppState perf edge

Accepted. `121bc87` closes my previous perf blocker:

- `LivingAsset` now guards `startLoop` while backgrounded/inactive and prevents duplicate loop creation.
- `WorkerSprite` and `CharacterPathLayer` no longer freeze cold-start unknown/null AppState by requiring only strict `"active"`.

I still prefer a small lifecycle test eventually, but the code-level defect from `89eeaaa` is addressed.

## Still Blocking 100

### P1 - Raw implementation errors still reach many screens

The targeted cleanup fixed audit/persona/inbox/wiki/settings paths, but all-screen review still finds many visible or likely visible raw error messages at `HEAD`, including:

- `src/app/account.tsx:83`, `src/app/account.tsx:115`
- `src/app/attachment.tsx:83`
- `src/app/capture.tsx:299`, `313`, `329`, `389`, `472`, `497`, `531`
- `src/app/big-five.tsx:92`
- `src/app/inbox.tsx:66`, `179`, `202`
- `src/app/import.tsx:109`
- `src/app/insights.tsx:40`
- `src/app/interview.tsx:144`, `189`
- `src/app/research.tsx:64`
- `src/app/trinity.tsx:134`
- `src/app/wiki.tsx:232`, `275`
- `src/components/wiki/AddFormatFlow.tsx:51`, `76`

100/100 requires product-tone user copy plus retry/recovery, with raw details logged only.

### P1 - Diagnosis disclosure policy is internally inconsistent

`src/app/persona.tsx:228-229` still shows:

- `진단이 아니며`
- `Not a diagnosis`

This passes CI because `src/lib/safety/lexicon.ts` explicitly allowlists landing/persona for a mandatory "not a diagnosis" disclosure. But `DESIGN.md` and `CLAUDE.md` say `diagnosis` / `진단`-class vocabulary is forbidden in product copy. This needs a deliberate product/legal decision.

I am not treating it as a build-breaker because the repo currently encodes it as mandatory. I am treating it as a 100-point design/trust blocker until the source of truth is reconciled.

### P1 - Consent/legal readiness still open

`src/components/consent/ConsentNotice.tsx` still states that legal wording is placeholder pending legal review. Prior consent-ledger readiness concerns remain open.

### P1 - Social provider and device proof remain open

Claude's own request notes these as unresolved. They remain outside the visual cleanup but inside launch-grade trust review.

### P2 - Small tap-target leftovers remain

Assessment scales are fixed, but small chip/editor `hitSlop={2}` remains at `HEAD`:

- `src/app/capture.tsx:693`, `src/app/capture.tsx:965`
- `src/app/wiki.tsx:661`
- `src/components/wiki/TemplateEditor.tsx:300`, `347`

Lower priority than assessment controls, but not 100.

## Current Score

**91/100 provisional.**

Why not higher:

- Major slop blockers closed: support vocab, visible mock tokens, Phase 1 labels, assessment tap targets, AppState perf edge.
- But all-screen raw errors, consent/legal readiness, provider/device proof, and source-of-truth conflict around diagnosis copy still prevent 100.

## Next

I detected a new uncommitted WIP after `121bc87`:

- `src/app/inbox.tsx`
- `src/app/records.tsx`
- `src/app/wiki.tsx`

It appears to be FlatList/perf work. I will not score it until committed or reported complete.

## Links

- HTML preview: `E:\Coding Infra\AI Infra\Communication\agents\codex\outbox\preview\20260606-021046-121bc87-regate.html`
