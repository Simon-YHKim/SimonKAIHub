# QA Report - 2026-06-14 (KST)

## 1. P0 Bug Verification
- **Issue**: Splash/Home hang due to `sources` query using `created_at` (non-existent column) in `index.tsx`.
- **Status**: **FIXED**.
- **Evidence**:
    - Verified `E:\2ndB\src\app\index.tsx` contains `captured_at` instead of `created_at`.
    - App successfully boots past Splash screen.
    - No `ReactNativeJS` errors about `created_at` in `logcat` after reload.
    - Screenshot `screen_dashboard.png` (928KB) confirmed Home screen is rendering.

## 2. Core Loop Test Status
- **Capture**: **PASS (Partial)**. 
    - Navigated to `/capture`.
    - Blindly entered text and submitted.
    - `logcat` confirmed `shard_capture_mint.png` loading, suggesting successful transition to capture flow.
- **Inbox**: **TBD**.
    - Deep-linked to `/inbox`. Screenshot taken.
    - Automated text verification blocked by P1 bug (see below).
- **Wiki**: **TBD**.
- **SecondB Chat**: **PASS (Partial)**.
    - Navigated to `/secondb`.
    - UI Dump extracted text: "Hi, I'm SecondB".
    - Intro modal visible and accessible.

## 3. New Findings / Regressions
- **[P1] uiautomator dump timeout**: `uiautomator dump` consistently fails with `ERROR: could not get idle state` on Home, Capture, and Inbox screens.
    - Only works on SecondB chat screen.
    - Disabling animations (`animator_duration_scale 0`) did not resolve the issue.
    - Suggests an infinite animation loop or background task keeping the main thread busy.
    - This blocks automated QA tools that rely on UI hierarchy.

## 4. Environment
- **Device**: emulator-5554
- **Project**: E:\2ndB
- **Commit**: `2467f3fd31395cc7d8714ee2885230bdfe1da00b`
